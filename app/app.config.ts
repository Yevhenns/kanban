export default defineAppConfig({
  ui: {
    colors: {
      primary: "emerald",
      neutral: "slate",
    },
    button: {
      defaultVariants: {
        size: "xl",
      },
    },
    input: {
      defaultVariants: {
        size: "xl",
      },
    },
  },
});
